import { createTheme } from "@mui/material/styles";

const tableStyle = createTheme({
  palette: {
    primary: {
      main: "#a11215",
    },
    secondary: {
      main: "#e3a314",
    },
  },
});

export default tableStyle;
